const width = window.innerWidth;
const height = window.innerHeight;

const motifWidth = 100;
const motifHeight = 100;

const numMotifsWidth = Math.floor(width / motifWidth);
const numMotifsHeight = Math.floor(height / motifHeight);